# Binaries / build outputs
/bin/
/dist/
*.exe
*.test
*.out

# Go modules: go.sum is committed, vendor/ is not.
vendor/

# Coverage
*.cover
coverage.txt
coverage.html

# Editor / OS
.DS_Store
._*
.idea/
.vscode/
*.swp
*~

# Local config that callers shouldn't ship
local_config.yaml
play/

# Cgo intermediate artifacts (rare with Go modules but harmless)
*.cgo*.go
*.cgo*.c
_cgo_*
